<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<import resource="classpath:spring/spring-activemq-common.xml"/>
	<!--&lt;!&ndash; 配置消息队列监听者（Queue） &ndash;&gt;
	<bean class="com.fan.listener.ActiveMqListener" id="activeMqListener"/>
	&lt;!&ndash; 显示注入消息监听容器（Queue），配置连接工厂，监听的目标是queueDestination，监听器是上面定义的监听器 &ndash;&gt;
	<bean id="queueListenerContainer" class="org.springframework.jms.listener.DefaultMessageListenerContainer">
		<property name="connectionFactory" ref="connectionFactory" />
		<property name="destination" ref="queueDestination" />
		<property name="messageListener" ref="activeMqListener"/>
	</bean>-->
</beans>
